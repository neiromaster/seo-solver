# Advanced fetch usage

```ts
import { createSharedRetryExecutor, registerBackend, resolveBackend } from '@seo-solver/fetch/advanced';
```

The advanced surface owns backend registry wiring and shared retry execution.
