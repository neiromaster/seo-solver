---
"seo-solver": minor
---

Add editor mode for `extract` and `compare`. `extract --editor` can open the generated JSON in supported editors, while `compare --editor` opens two normalized JSON artifacts in diff mode without changing normal `--format` and `--output` behavior.
